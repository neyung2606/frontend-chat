import React from 'react'
import { SvgIcon } from "@material-ui/core";
import icongoogle from '@assets/google.svg'

const GoogleIcon = (props) => {
  return (
    <SvgIcon component={icongoogle} />
  );
};

export { GoogleIcon }